
#include <bits/stdc++.h>

#include "model.h"
#include "utils.h"

using namespace std;

void populate_percentile_hotness(std::vector<REGION_SKD *> &regions) {

    // supress compiler warning unused var
    (void)regions;

    throw "Replace with convolved percentile";
}
